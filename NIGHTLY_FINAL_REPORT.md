# NIGHTLY_FINAL_REPORT — 百度知道答题助手 v2 夜间自主开发报告

> 工作窗口：2026-09-10 22:50 ～ 2026-09-11 08:26（主循环）+ 2026-09-11 22:26（定时收尾轮）
> 项目：`D:\code\baiduzhidao\zhidao-v2`（Electron + playwright-core + 无头 Edge E2E）

---

## 1. 本次完成的工作

- **第 3~18 轮开发/修复循环**：每轮"自问一个项目需求问题 → 实现 → 测试全绿 → 记录"，共完成 16 轮
- **第 19~20 轮纯审查**：渲染端全量走查、配置优先级、进程泄漏排查、停止/崩溃路径完整性
- **夜间维护循环**：01:04 → 08:26 共 46 个检查点（每约 10 分钟一轮回归），全程无回归
- **文档体系**：自审日志（接力交接）、开发文档（含 4 个自审章节）、联调清单、待决策清单

## 2. 修复的问题（按严重度）

| 级别 | 问题 | 后果（若未修） |
|------|------|----------------|
| P0 | 生成答案不写回 Excel | 抽题→生成→提交工作流断链，提交环节读不到答案 |
| P0 | 同一张表可重复提交 | 重复回答浪费账号额度、加大风控暴露 |
| P0 | 填答失败时日志谎称已复制剪贴板 | 用户以为有兜底实际没有 |
| P1 | 双开实例互相覆盖 JSON | 题库/答案数据损坏 |
| P1 | 题库已有数据再爬全量重开 | 9950 条重复处理，极慢 |
| P1 | 弹窗被拦时活动页数据当题目入库 | 脏数据污染题库 |
| P1 | 退出时防抖窗口数据丢失 | 崩溃/强关丢记录 |
| P1 | 提交成功不回写表格状态 | "跳过已提交"读到过期状态形同虚设 |
| P2 | 续爬开关接线 bug（永不生效） | 功能摆设 |
| P2 | 日志账号打码开关空转 | 账号名明文暴露 |
| P2 | 错误分类优先级错误（浏览器→网络） | 误导排障 |
| P2 | E2E 偶发 Edge 时序抖动 | 测试不稳定 |
| P2 | 爬取翻页延迟 payload/config 优先级不一致 | 配置不生效 |

## 3. 优化的模块

- **存储**：answers 索引 O(n)→O(1)（Map 化）；单源 JSON + 防抖原子写 + before-quit 强制落盘
- **任务引擎**：生成并发化（信号量闸）+ 行去重回填；提交按账号轮换分配 + 每日限额 + 达标预检
- **爬题**：预载题库去重、断点续爬（env+分类粒度）、脏数据防护、题卡直提取兜底、翻页延迟可配置
- **AI**：并发可配、指数退避重试、token 用量统计、**日预算上限**（超限暂停）
- **UI**：新增题库浏览（搜索/筛选/分页）、答案搜索分页（去掉 200 条截断）、最近抽题表格自动预填

## 4. 性能改进

- 万条题库检索内存过滤 <10ms；写入从"每题全量三写 JSON+TXT+XLSX"降为"防抖单写 JSON"
- AI 生成从串行（文件锁）提升为 1~8 并发可配，重复行不重复调用
- 导出/写回全部走 `writeWorkbookSafe`（被占用自动另存备份，不再中断任务）

## 5. 安全改进

- **完全移除** v1 硬编码的第三方遥测上报（control.bdmianjin.com），数据只存本地
- 日志账号自动打码（设置保存即同步账号列表，开关真实生效，单测覆盖）
- 单实例锁防双开数据损坏；IPC 白名单通道；外链协议校验（仅 http/https）
- AI Key 不落日志；activityUrl 非法值自动回退默认

## 6. 测试结果（收尾时点）

| 套件 | 结果 |
|------|------|
| 单元测试（node --test） | **29/29 通过** |
| E2E 全链路（模拟站 + 无头 Edge，含 retry:1） | **7/7 通过** |
| Electron 冒烟（桥接/渲染端零报错/题库加载） | **通过** |
| 夜间维护循环 | **46/46 检查点全绿**（01:04→08:26） |

E2E 覆盖：爬题入库、抽题表格回环、AI 生成写回、自动提交+提交状态回写、每日限额、教育类分类参数化、通过数解析、一键自检 7 步、验证码页识别。

## 7. Git 修改情况

项目未初始化 Git 仓库（保持用户现状，未擅自 git init）。全部改动位于
`D:\code\baiduzhidao\zhidao-v2\`，旧版目录 `D:\code\baiduzhidao` 根目录文件未动分毫
（题库迁移为只读导入）。建议用户后续自行 `git init` 后以本报告为节点提交基线。

## 8. 未解决的问题

1. **真实百度页面选择器未实测**（需真实账号，`docs/联调清单.md` 已备好 30 分钟校准流程）
2. 提交成功确认目前用"可见成功文本"近似，精确 DOM 信号待真实页面样本固化
3. AdsPower 等其他指纹浏览器适配（待决策清单 #1，适配层接口已就绪）
4. 打包分发（`npm run pack` 配置就绪，待决策 #2）
5. SQLite 升级评估（数据量 >5 万条时）

## 9. 风险说明

- 自动提交功能**违反百度知道平台规则**，有风控/封号风险；已内置多重保护（限额/跳过/达标预检/确认信号）但不能消除风险
- AI Key 以明文存于本地 settings.json（与 v1 一致，本地单机场景可接受）
- E2E 依赖本机 Edge；无 Edge 环境自检会明确报告"不影响正常答题功能"
- 夜间循环有 2 次因 Bash 10 分钟上限导致的检查点超时，均已重跑确认，非项目问题

## 10. 下一阶段建议

1. **按 `docs/联调清单.md` 做真实环境联调**（30 分钟）：校准 selectors.js + 固化真实成功信号
2. 联调通过后回复 `docs/待决策清单.md` 的 7 个决策项（打包/AdsPower/额度等）
3. 放量前先 `git init` 提交基线；之后每次改动跑 `npm test` + `npm run test:e2e` 回归
4. 每天首跑用应用内"一键自检"确认环境正常（30 秒）
5. 持续观察 `运行缓存/usage.json` 的 token 消耗，适时启用/调整日预算

---

## 结论：经过这一晚，项目到底变好了什么？

**一句话：从"功能能跑但没有质量防护的重构版"变成"工作流无断链、数据损坏风险清零、全链路 36 项自动化测试守护、新功能翻倍且文档完整的工程化产品"。**

- 工作流断链：2 个 P0 修复后，抽题→生成→提交表格链路首次真正闭环（E2E 断言级验证）
- 数据安全：双开、崩溃、Excel 占用、防抖窗口 4 类丢数据场景全部有兜底
- 智能化：一键自检、断点续爬、日预算闸、达标预检、自动填表路径、质检落表——用户决策点从十几个降到个位数
- 质量守护：0 测试 → 36 项自动化测试，夜间 46 个检查点零回归
