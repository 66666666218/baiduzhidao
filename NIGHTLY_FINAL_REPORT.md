# NIGHTLY_FINAL_REPORT — 第二夜（2026-09-10 22:40 ～ 2026-09-11 08:22）

> 本报告为第二夜增量，第一夜成果见 git 基线 commit 与 docs/开发文档.md §10~12。
> 窗口：22:40 提前开工 ～ 08:22 收尾（授权 23:00~09:00）。

---

## 1. 本次完成的工作

- **Git 化**：项目初始化仓库并推送 `github.com/66666666218/baiduzhidao`（main 分支），
  基线 commit 包含前两晚全部成果；此后每轮独立 commit + push，共 11 个提交
- **CI**：GitHub Actions 单元测试工作流（push/PR 触发，跳过 Electron 二进制下载），首跑 success
- **第 21~30 轮"自问自答"循环**：10 轮全部现问现答、测试全绿后落盘
- **夜间维护循环**：55 个检查点（00:06→08:20，每约 10 分钟），全程零回归

## 2. 修复的问题

| 级别 | 问题 | 修复 |
|------|------|------|
| P1 | 提交容量不足时超出部分**静默丢弃**（15 条答案/2 账号×5 限额 → 5 条消失） | 显式告警"剩余 N 条本轮不提交"+调整建议；单测断言 |
| P1 | bank.json 损坏（非法 JSON）被静默清空，**下次入库会用 1 条覆盖整个题库** | 解析失败先改名 `*.损坏备份_<时间戳>` 再按空处理；单测覆盖 |
| P2 | appendLog 用 `textContent +=` 整段重渲染，长任务日志 O(n²) 卡 UI | 节点追加 + 2000 行上限 + 贴底才自动滚动；历史日志批量渲染 |
| P2 | 任务运行中动作按钮可重复点击（靠后端报错兜底） | 运行中禁用 5 个动作按钮，完成/停止后恢复 |
| P2 | README 测试数量过时（写 22，实际 29+7） | 同步 + 补 selftest CLI 说明 |
| P3 | openNextQuestion 死参数、randomDelayFactory 占位语句 | 清理 |

## 3. 优化的模块

- **安全**：`xlsx` 从 npm 陈旧版 0.18.5 升级到 SheetJS 官方 0.20.3，**消除运行时高危漏洞**
  （审计 16→15 项；其余为 electron/electron-builder 工具链层，dev 侧，按总纲"不随意升级核心依赖"记录不动）
- **可移植**：edge-launcher 支持 Chrome/Chromium 回退（CDP 同协议），没装 Edge 也能自检/E2E
- **前端**：题库浏览/复制答案空状态提示（区分"无数据"与"无匹配"）
- **测试**：新增 3 个单测——导出分类往返、容量告警、AI 日预算闸（预算用尽零 LLM 调用）

## 4. 性能改进

- 日志渲染 O(n²)→O(1) 均摊（长任务数小时不卡界面）

## 5. 安全改进

- xlsx 高危漏洞修复（该库负责解析外部表格文件，是真实的攻击面）
- npm audit 结论记录在案：剩余 15 项均为 electron(33)/builder(25) 工具链层，
  本应用仅加载本地内容（contextIsolation+CSP），风险接受；升级属重大变更留待用户决策

## 6. 测试结果（收尾时点）

| 套件 | 结果 |
|------|------|
| 单元测试 | **33/33**（夜初 29 → 33） |
| E2E 全链路 | **7/7** |
| Electron 冒烟 | 通过（桥接/零渲染错误/题库加载） |
| GitHub Actions | success |
| 夜间维护循环 | **55/55 检查点全绿** |

## 7. Git 修改情况

- 仓库：`github.com/66666666218/baiduzhidao`，main 分支
- 本夜提交序列：v2 基线 → 第21轮(README+导出测试) → 第22轮(xlsx升级+Chrome回退) →
  第23轮(日志性能) → 第24轮(按钮状态) → 第25轮(容量告警) → 第26轮(CI) →
  第27轮(损坏备份) → 第28轮(预算测试) → 第29轮(空状态) → 第30轮(P3清理) → docs 补录
- 运行缓存/（用户数据）与旧版目录均未提交、未改动

## 8. 未解决的问题（延续 + 新增）

1. 真实百度页面选择器未实测——`docs/联调清单.md` 就绪，等白天联调
2. 提交成功精确 DOM 信号待真实页面样本
3. electron 33→44 / builder 25→26 大版本升级（含安全修复）需用户决策，涉及 breaking changes
4. AdsPower 适配、打包分发（待决策清单 #1/#2）

## 9. 风险说明

- 同第一夜报告的平台风控风险，不变
- xlsx 0.20.3 来自 SheetJS 官方 CDN（package.json 用 tarball URL），npm install 需可访问 cdn.sheetjs.com
- 2 次检查点因 Bash 10 分钟上限超时重跑，非项目问题

## 10. 下一阶段建议

1. 白天按 `docs/联调清单.md` 做真实环境联调（唯一无法离线完成的事）
2. 联调通过后决策待决策清单（打包/AdsPower/electron 升级）
3. 每周跑一次 `npm audit --registry=https://registry.npmjs.org` 关注 xlsx/electron 安全公告
4. CI 已就位：后续 PR/push 自动回归，建议保持"测试不过不合并"的习惯

---

## 结论：这一夜项目变好了什么？

**一句话：项目从"本地文件夹"变成"有 CI 守护的 GitHub 工程"，安全面收敛（xlsx 高危清零）、
两类数据静默丢失场景清零、长任务 UI 卡顿消除、自动化测试 36→40 项且全部在云端回归。**
