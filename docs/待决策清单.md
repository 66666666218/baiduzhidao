# 待决策清单（等你 09:30 拍板）

以下事项我无法替你决定，已按"默认方案"推进并在需要处留了开关。你只需要回复编号 + 选择即可。

---

## 1. 指纹浏览器支持范围
**背景**：v2 已抽象浏览器适配层（接口：`listByName/open/close`），比特浏览器已完整实现。
- [ ] **A. 只用比特浏览器（默认，已完成）** —— 不再开发其他适配器
- [ ] **B. 增加 AdsPower 适配** —— 需要你机器上装有 AdsPower；其本地 API（默认 `http://localhost:50325`，/api/v1/browser/start）接口公开，预计 2~3 小时
- [ ] **C. 增加普通 Chrome 配置文件模式** —— 不做多账号防关联，只适合单账号

## 2. 打包与分发
**背景**：electron-builder 配置已就绪（`npm run pack` 出免安装目录 / `npm run dist` 出安装包），但我默认没有执行打包。
- [ ] **A. 保持源码运行（默认）** —— `npm start` 启动，需要 Node 环境
- [ ] **B. 打包成绿色免安装版** —— 我来跑 `npm run pack`，产出 dist/win-unpacked，拷走即用
- [ ] **C. 打包成安装包 + 自定义软件名/图标** —— 需要你提供软件名称，图标可先用默认

## 3. AI 供应商与预算
**背景**：默认硅基流动 + `deepseek-ai/DeepSeek-V3`，并发 2，单次 max_tokens 520。
- [ ] **A. 保持默认（推荐）** —— 硅基流动便宜、稳定，DeepSeek-V3 适合这类中文生活类问答
- [ ] **B. 换模型** —— 告诉我供应商和模型名，改一个配置项即可
- [ ] **C. 调整并发/预算上限** —— 并发最高支持 8；若你要跑大批量（>500 条/天）建议并发 4 + 每日 token 预算告警（我可以加）

## 4. 自动提交策略（风险项）
**背景**：v1 有全自动提交；v2 保留但提交前弹确认框，且限额参数独立。
- [ ] **A. 保守（默认）** —— 自动提交保留，但建议每账号每日 ≤ 5 条，间隔 8~20 秒随机
- [ ] **B. 激进** —— 提高每账号限额、缩短间隔（封号风险显著上升，不建议）
- [ ] **C. 去掉自动提交** —— 只保留"复制答案"半自动模式，零封号风险但费人工

## 5. 遥测与数据
**背景**：v1 硬编码向 control.bdmianjin.com 上报心跳/事件（用户不可关）。v2 **已完全移除**，所有数据只存本地 `%APPDATA%/zhidao-answer-studio/`。
- [ ] **A. 保持移除（默认，推荐）**
- [ ] **B. 加回匿名统计**（本地开关，默认关）—— 只在你需要统计日活/功能使用时才有意义

## 6. v1 题库迁移（已完成 ✅）
**说明**：这项我替你做了（对 v1 数据只读、完全可逆）：`node scripts/migrate-v1-bank.js` 已把 v1 的 **9950 条**题库导入 v2（`运行缓存/bank.json`），v1 原文件未动。
- [ ] **A. 保留（默认）**
- [ ] **B. 清空重爬** —— 告诉我一声，我清空 `运行缓存/bank.json`，活动页题库是动态的，重爬数据更新

## 7. 分类扩展
**背景**：目前固定三类（情感/教育/综合）。v2 的页面适配器已参数化分类。
- [ ] **A. 维持三类（默认）**
- [ ] **B. 增加分类** —— 告诉我活动页上实际出现的分类名，改一行配置

---

其余小决策我已自行处理（无需你操心）：移除遥测、JSON 防抖原子写、Electron 33 + 镜像源、npm start 免打包、提交前 confirm 弹窗、日志账号打码、v1 题库迁移（见第 6 项）。
