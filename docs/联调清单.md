# 真实环境联调清单（白天按此顺序操作，预计 30 分钟）

> **联调状态（2026-09-14 凌晨）：✅ 已完成**
> - 选择器校准：活动页/题目页全部真实结构已固化（第39~41轮）
> - 真实爬题：3 条情感类题目入库 ✓
> - 真实填答：textarea.cos-textarea-box 202 字填写 ✓
> - **真实提交：成功发布，真实成功信号 =「回答已发布」**（已固化进 readSubmitConfirmation）
> - 提交后重访页面：回答已展示 ✓
> - 剩余：AI 生成真实调用（等 API Key，配置即可用）

## 0. 前置（5 分钟）

- [ ] 启动应用：`cd D:\code\baiduzhidao\zhidao-v2 && npm start`
- [ ] 点"一键自检"——7 步全过说明引擎、Edge、模拟链路正常（与真实百度无关）
- [ ] 打开比特浏览器客户端 → 本地设置 → 开启本地 API（默认 54345）
- [ ] 应用"比特环境"页 → 测试连接（应显示 ✅）→ 填 1 个账号名 → 保存设置

## 1. 爬题校准（10 分钟，最关键）

- [ ] 题目爬取页 → 开始爬取（题库已有 9950 条会自动跳过重复，只抓新题）
- [ ] 观察运行日志：
  - ✅ `已进入答题区` → `已锁定分类：情感类` → `打开题目：…` → `已获取题目地址：…`
  - ❌ 卡在 `等待活动页加载` → 页面结构变化，需要更新 `electron/src/pages/selectors.js` 的 zone 段
  - ❌ `未识别到"情感类"标签` → 分类文本变了，更新 `SEL.category.labels`
  - ❌ 大量 `跳过无效题目页（标题不匹配）` → 详情页标题选择器变了，更新 `SEL.question.titleCandidates`
- [ ] 到"题库浏览"抽查新爬的题目链接能否打开

## 2. AI 生成校准（5 分钟）

- [ ] 生成答案页 → 填硅基流动 API Key → 保存设置（建议顺手设"今日 token 预算"如 50000）
- [ ] 随机抽题 2 条 → 生成答案（来源表格已自动预填）
- [ ] 检查：表格"质检"列、回答长度与语气是否合格（不合格调 prompt：设置文件 systemPrompt）

## 3. 提交试水（10 分钟，高风险环节）

- [ ] **确认这是要真实发布的账号，额度调最小**：提交数量上限填 1
- [ ] 自动提交 → 确认弹窗 → 观察日志：
  - ✅ `答案已填入（方式：…）` + `已确认页面成功提示：「…」` → 记录真实的成功信号文案，告诉我，我把它固化进 `selectors.js`
  - ❌ `没有找到回答输入框` → 把详情页 HTML 发我，我更新编辑器选择器
  - ❌ `未捕捉到页面成功提示` → 正常现象之一，人工到账号页核对该回答是否真的发布
- [ ] 到百度知道账号页确认回答可见

## 4. 收尾（把结果告诉我）

按联调结果回报：哪些日志是 ✅、哪些 ❌、成功提示的真实文案。我据此固化选择器与确认信号，
之后就可以放量（调高每账号每日限额、增加账号）。

## 风险红线（务必遵守）

- 首日每账号提交 ≤ 5 条，观察 24h 无异常再放量
- 操作间隔保持默认 8~20 秒随机，不要调小
- 遇到安全验证立即停下等人工处理，绝不要连续重试
